package com.stslex.wizard.feature.follower.navigation

import com.stslex.wizard.core.navigation.navigator.Navigator
import com.stslex.wizard.feature.follower.ui.store.FollowerStore.Action.Navigation

class FollowerRouterImpl(private val navigator: Navigator) : FollowerRouter {

    override fun invoke(event: Navigation) {
        TODO("Not yet implemented")
    }
}